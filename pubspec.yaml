name: ai_field_assistant
description: "Nền tảng ứng dụng mobile-first ghi nhận sự cố hiện trường."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

dependencies:
  firebase_ai: ^4.0.0
  firebase_app_check: ^0.4.8
  firebase_core: ^4.15.0
  flutter:
    sdk: flutter
  image_picker: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
